<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/AppBaseTheme" />

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Navigation Bar Color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Notification Bar Color -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Fonts Color -->
        <item name="android:textColorPrimary">@color/Black</item>
        <!-- Solve TextView global up/lower case issue -->
        <item name="android:textAllCaps">false</item>
        <!-- Arrow in toolbar with animation, set animation type and color -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <!-- Color for text in menu in toolbar -->
        <item name="actionMenuTextColor">@color/White</item>
        <item name="android:windowAnimationStyle">@style/pageInOut</item>
    </style>

    <!-- No ActionBar theme -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="android:textColor">#000</item>
        <item name="colorPrimary">#D33A31</item>
        <item name="colorPrimaryDark">#D33A31</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--set item color in menu-->
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>

    <style name="pageInOut">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_from_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_to_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_from_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_to_right</item>
    </style>

    <!-- Left Arrow Style -->
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

</resources>
